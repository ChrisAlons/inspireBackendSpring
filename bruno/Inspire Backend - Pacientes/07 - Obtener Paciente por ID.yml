info:
  name: 07 - Obtener Paciente por ID
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/pacientes/{{pacienteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
