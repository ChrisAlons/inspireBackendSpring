info:
  name: 15 - Verificar eliminado → 404
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/api/pacientes/{{pacienteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
