services:
  postgres:
    image: 'postgres:${POSTGRES_IMAGE_TAG:-18.3-alpine3.23}'
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mydatabase}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secret}
      POSTGRES_USER: ${POSTGRES_USER:-myuser}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
